---
phase: 02-configurator-and-inquiry-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - components/configurator-wizard.tsx
  - app/configure/page.tsx
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04

must_haves:
  truths:
    - "User navigates 5 steps: Chassis, Motor, Discos, Sourcing, then Contact + Summary"
    - "Each step 1-4 shows option cards as accessible radio buttons (role=radio inside role=radiogroup)"
    - "Next button is disabled when no option selected in current step"
    - "User can navigate back to any prior step without losing selections already made"
    - "Step 5 Zone A shows a summary of all 4 selections with CHANGE links that navigate to that step"
    - "Step 5 Zone B shows contact form (Name required, Email required, Message optional)"
    - "Successful submit replaces wizard with success state showing INQUIRY SENT heading and configuration summary"
    - "/configure route renders with 'BUILD YOUR LAMBRE-BULL' heading and price reference"
  artifacts:
    - path: "components/configurator-wizard.tsx"
      provides: "5-step configurator wizard Client Component"
      contains: "'use client'"
      exports: ["ConfiguratorWizard"]
    - path: "app/configure/page.tsx"
      provides: "Server Component page shell for /configure route"
      contains: "ConfiguratorWizard"
  key_links:
    - from: "components/configurator-wizard.tsx"
      to: "app/actions/configure.ts"
      via: "submitInquiry.bind(null, selections) inside component body"
      pattern: ".bind(null, selections)"
    - from: "components/configurator-wizard.tsx"
      to: "lib/configurator.ts"
      via: "ConfiguratorOptions prop from page"
      pattern: "ConfiguratorOptions"
    - from: "app/configure/page.tsx"
      to: "components/configurator-wizard.tsx"
      via: "<ConfiguratorWizard options={configuratorOptions} />"
      pattern: "ConfiguratorWizard"
---

<objective>
Build the 5-step configurator wizard Client Component and the /configure page shell. This is the complete buyer-facing configurator: option selection, free navigation, summary review, contact form, submission, and success state.

Purpose: Deliver CONF-01 through CONF-04 end-to-end. A buyer selects chassis/motor/discos/sourcing, reviews their build, and submits a structured inquiry in one uninterrupted flow.
Output: components/configurator-wizard.tsx (all wizard logic + UI), app/configure/page.tsx (Server Component page shell).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-configurator-and-inquiry-integration/02-CONTEXT.md
@.planning/phases/02-configurator-and-inquiry-integration/02-RESEARCH.md
@.planning/phases/02-configurator-and-inquiry-integration/02-PATTERNS.md
@.planning/phases/02-configurator-and-inquiry-integration/02-UI-SPEC.md
@.planning/phases/02-configurator-and-inquiry-integration/02-01-SUMMARY.md
@.planning/phases/02-configurator-and-inquiry-integration/02-02-SUMMARY.md

<interfaces>
<!-- Exact contracts the executor needs. Do not re-explore the codebase for these. -->

From lib/configurator.ts (Plan 01 output):
```typescript
export type ConfigOption = {
  id: string
  label: string
  description: string
}
export type ConfiguratorOptions = {
  chassis: ConfigOption[]
  motor: ConfigOption[]
  discos: ConfigOption[]
  sourcing: ConfigOption[]
}
export type ConfigSelections = {
  chassis: string | null
  motor: string | null
  discos: string | null
  sourcing: string | null
}
export const configuratorOptions: ConfiguratorOptions
export function getOptionLabel(category: keyof ConfiguratorOptions, id: string): string
```

From app/actions/configure.ts (Plan 02 output):
```typescript
export type ConfigFormState = {
  status: 'idle' | 'success' | 'error'
  errors?: {
    name?: string[]
    email?: string[]
    message?: string[]
    chassis?: string[]
    motor?: string[]
    discos?: string[]
    sourcing?: string[]
  }
  message?: string
}
export async function submitInquiry(
  selections: ConfigSelections,
  _prevState: ConfigFormState,
  formData: FormData,
): Promise<ConfigFormState>
```

From components/contact-form.tsx (exact patterns to replicate in wizard):
```typescript
// SubmitButton must be a separate function component — CRITICAL
// useFormStatus() reads from nearest ancestor <form>; if in same component as <form>, always returns { pending: false }
function SubmitButton() {
  const { pending } = useFormStatus()
  return (
    <Button type="submit" disabled={pending}
      className="w-full bg-[#cc2200] hover:bg-[#a81c00] text-[#f2f2ee] uppercase tracking-widest font-black px-6 py-3 rounded-sm transition-colors min-h-[44px] disabled:opacity-40 disabled:cursor-not-allowed"
      style={{ fontFamily: 'var(--font-barlow-condensed)' }}
    >
      {pending ? 'SENDING...' : 'SUBMIT INQUIRY'}
    </Button>
  )
}

// Field + error pattern
<div className="flex flex-col gap-2">
  <Label htmlFor="id" className="text-xs uppercase tracking-widest text-[#f2f2ee]">Field</Label>
  <Input id="id" name="name" required maxLength={100}
    aria-invalid={!!errors.fieldName}
    aria-describedby={errors.fieldName ? 'id-error' : undefined}
    className="bg-[rgba(242,242,238,0.06)] border border-[rgba(242,242,238,0.12)] text-[#f2f2ee] rounded-sm focus-visible:border-[#cc2200] focus-visible:ring-1 focus-visible:ring-[#cc2200]"
  />
  {errors.fieldName && (
    <p id="id-error" className="text-sm text-[oklch(0.65_0.22_27)]">{errors.fieldName[0]}</p>
  )}
</div>

// aria-live feedback region
<div aria-live="polite" aria-atomic="true" className="min-h-[1.5rem]">
  {state.status === 'success' && state.message && (
    <p className="text-sm text-[#f2f2ee]">{state.message}</p>
  )}
  {state.status === 'error' && state.message && (
    <p className="text-sm text-[oklch(0.65_0.22_27)]">{state.message}</p>
  )}
</div>
```

From app/contact/page.tsx (exact page.tsx analog):
```typescript
import { Suspense } from 'react'
import { connection } from 'next/server'  // NOTE: configure/page.tsx does NOT need connection() — no dynamic input

export default function ContactPage({ ... }) {
  return (
    <main className="max-w-lg mx-auto px-4 py-16">
      <Suspense fallback={null}>
        <ContactContent ... />
      </Suspense>
    </main>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/configurator-wizard.tsx</name>
  <files>components/configurator-wizard.tsx</files>
  <read_first>
    - components/contact-form.tsx — full file; SubmitButton pattern, useActionState pattern, field+error pattern, aria-live pattern must be copied exactly
    - .planning/phases/02-configurator-and-inquiry-integration/02-UI-SPEC.md — full file; this is the locked visual contract. Every className, copy string, and interaction behavior comes from here.
    - .planning/phases/02-configurator-and-inquiry-integration/02-RESEARCH.md — Pattern 2 (direction-aware animation), Pattern 3 (accessible radio card), Pitfall 1-6 (ALL must be read before writing)
    - .planning/phases/02-configurator-and-inquiry-integration/02-PATTERNS.md — "configurator-wizard.tsx" section for the complete list of additions beyond contact-form.tsx
    - node_modules/next/dist/docs/01-app/02-guides/forms.md — "Passing additional arguments" section (the .bind() pattern)
    - app/globals.css — color tokens, font variables
  </read_first>
  <action>
Create components/configurator-wizard.tsx. This is a large Client Component (~200-250 lines). Read ALL reference files first. Then implement in order:

**1. Directive and imports:**
```typescript
'use client'

import { useState, useRef, useEffect } from 'react'
import { useActionState } from 'react'
import { useFormStatus } from 'react-dom'
import { Check } from 'lucide-react'
import { cn } from '@/lib/utils'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Textarea } from '@/components/ui/textarea'
import { Separator } from '@/components/ui/separator'
import { submitInquiry, type ConfigFormState } from '@/app/actions/configure'
import type { ConfiguratorOptions, ConfigSelections } from '@/lib/configurator'
import { getOptionLabel } from '@/lib/configurator'
```

**2. Constants:**
```typescript
const TOTAL_STEPS = 5

const STEP_NAMES: Record<number, string> = {
  1: 'CHASSIS',
  2: 'MOTOR',
  3: 'DISCOS',
  4: 'SOURCING',
  5: 'CONTACT + SUMMARY',
}

const STEP_CATEGORIES: Record<number, keyof ConfiguratorOptions> = {
  1: 'chassis',
  2: 'motor',
  3: 'discos',
  4: 'sourcing',
}

const STEP_HEADINGS: Record<number, string> = {
  1: 'CHOOSE YOUR CHASSIS',
  2: 'CHOOSE YOUR MOTOR',
  3: 'CHOOSE YOUR DISCOS',
  4: 'CHOOSE YOUR SOURCING',
  5: 'REVIEW AND SUBMIT',
}

const initialSelections: ConfigSelections = {
  chassis: null,
  motor: null,
  discos: null,
  sourcing: null,
}

const initialState: ConfigFormState = { status: 'idle' }
```

**3. SubmitButton child component (CRITICAL — must be a separate function, not inline):**
```typescript
function SubmitButton() {
  const { pending } = useFormStatus()
  return (
    <Button
      type="submit"
      disabled={pending}
      className="w-full bg-[#cc2200] hover:bg-[#a81c00] text-[#f2f2ee] uppercase tracking-widest font-black px-6 py-3 rounded-sm transition-colors min-h-[44px] disabled:opacity-40 disabled:cursor-not-allowed"
      style={{ fontFamily: 'var(--font-barlow-condensed)' }}
    >
      {pending ? 'SENDING...' : 'SUBMIT INQUIRY'}
    </Button>
  )
}
```

**4. Main ConfiguratorWizard component:**
```typescript
export function ConfiguratorWizard({ options }: { options: ConfiguratorOptions }) {
  const [currentStep, setCurrentStep] = useState(1)
  const [selections, setSelections] = useState<ConfigSelections>(initialSelections)
  const [direction, setDirection] = useState<'forward' | 'backward'>('forward')
  const successRef = useRef<HTMLHeadingElement>(null)

  // .bind() called INSIDE component body — captures current selections on every render
  const boundAction = submitInquiry.bind(null, selections)
  const [state, formAction] = useActionState(boundAction, initialState)

  // Focus management: move focus to success heading on success
  useEffect(() => {
    if (state.status === 'success') {
      successRef.current?.focus()
    }
  }, [state.status])

  function goToStep(step: number) {
    setDirection(step > currentStep ? 'forward' : 'backward')
    setCurrentStep(step)
    // NEVER reset selections here — free navigation (D-03) only changes current step
  }

  function handleNext() {
    setDirection('forward')
    setCurrentStep((s) => s + 1)
  }

  function handleBack() {
    setDirection('backward')
    setCurrentStep((s) => s - 1)
  }

  const errors = state.errors ?? {}
  const animationClass = direction === 'forward'
    ? 'animate-in fade-in-0 slide-in-from-right-4 duration-150'
    : 'animate-in fade-in-0 slide-in-from-left-4 duration-150'

  // SUCCESS STATE: replaces entire wizard
  if (state.status === 'success') {
    return (
      <div className="animate-in fade-in-0 duration-300">
        <h2
          ref={successRef}
          tabIndex={-1}
          className="text-3xl font-black tracking-tight uppercase text-[#f2f2ee]"
          style={{ fontFamily: 'var(--font-barlow-condensed)' }}
        >
          INQUIRY SENT
        </h2>
        <p className="mt-2 text-base text-[#888880] leading-normal">
          We've received your custom build inquiry and will be in touch shortly.
        </p>
        {/* Read-only configuration summary — same Zone A format, no CHANGE links */}
        <div className="mt-6 bg-[#1a1a1a] rounded-sm border border-[rgba(242,242,238,0.12)] p-6 flex flex-col gap-4">
          {(['chassis', 'motor', 'discos', 'sourcing'] as const).map((cat, i) => (
            <div key={cat}>
              {i > 0 && <Separator className="mb-4 bg-[rgba(242,242,238,0.12)]" />}
              <div className="flex justify-between items-center">
                <span className="text-sm uppercase tracking-widest text-[#888880]">{cat}</span>
                <span
                  className="text-base font-black uppercase text-[#f2f2ee]"
                  style={{ fontFamily: 'var(--font-barlow-condensed)' }}
                >
                  {selections[cat] ? getOptionLabel(cat, selections[cat]!) : '—'}
                </span>
              </div>
            </div>
          ))}
          <Separator className="bg-[rgba(242,242,238,0.12)]" />
          <p className="text-sm text-[#888880]">AU$18,000 – AU$25,000</p>
        </div>
        <a
          href="/"
          className="mt-8 inline-block text-sm font-black uppercase tracking-widest text-[#cc2200] hover:text-[#a81c00] transition-colors"
          style={{ fontFamily: 'var(--font-barlow-condensed)' }}
        >
          ← Back to Bikes
        </a>
      </div>
    )
  }

  return (
    <div>
      {/* Progress indicator */}
      <nav aria-label="Configurator progress" className="mb-6">
        <p
          className="text-sm font-black uppercase tracking-widest"
          style={{ fontFamily: 'var(--font-barlow-condensed)' }}
        >
          <span className="text-[#888880]">STEP {currentStep} OF {TOTAL_STEPS} — </span>
          <span className="text-[#cc2200]">{STEP_NAMES[currentStep]}</span>
        </p>
      </nav>

      {/* Step content — key drives animation on every step change */}
      <div key={`step-${currentStep}-${direction}`} className={animationClass}>
        <h2
          className="text-3xl font-black tracking-tight uppercase text-[#f2f2ee] mb-8"
          style={{ fontFamily: 'var(--font-barlow-condensed)' }}
        >
          {STEP_HEADINGS[currentStep]}
        </h2>

        {/* Steps 1-4: option card grid */}
        {currentStep <= 4 && (() => {
          const category = STEP_CATEGORIES[currentStep]
          const categoryOptions = options[category]
          const currentSelection = selections[category]

          return (
            <div
              role="radiogroup"
              aria-label={`Choose your ${category}`}
              className="grid grid-cols-1 md:grid-cols-2 gap-4"
            >
              {categoryOptions.map((option) => {
                const isSelected = currentSelection === option.id
                return (
                  <button
                    key={option.id}
                    type="button"
                    role="radio"
                    aria-checked={isSelected}
                    aria-label={`Select ${category}: ${option.label}`}
                    onClick={() => setSelections((prev) => ({ ...prev, [category]: option.id }))}
                    className={cn(
                      'text-left p-4 rounded-sm min-h-[80px] transition-all duration-150',
                      isSelected
                        ? 'bg-[#1a1a1a] ring-2 ring-[#cc2200]'
                        : 'bg-[#1a1a1a] ring-1 ring-[rgba(242,242,238,0.12)] hover:bg-[#2a2a2a] hover:ring-[rgba(242,242,238,0.20)]'
                    )}
                  >
                    <div className="flex justify-between items-start">
                      <span
                        className="text-base font-black uppercase text-[#f2f2ee] leading-snug"
                        style={{ fontFamily: 'var(--font-barlow-condensed)' }}
                      >
                        {option.label}
                      </span>
                      {isSelected && (
                        <Check size={16} className="text-[#cc2200] flex-shrink-0 ml-2" aria-hidden="true" />
                      )}
                    </div>
                    <p className="mt-1 text-sm text-[#888880] leading-normal">{option.description}</p>
                  </button>
                )
              })}
            </div>
          )
        })()}

        {/* Step 5: Summary (Zone A) + Contact form (Zone B) */}
        {currentStep === 5 && (
          <div className="flex flex-col gap-8">
            {/* Zone A — Configuration summary */}
            <div>
              <h3
                className="text-3xl font-black uppercase text-[#f2f2ee] mb-4"
                style={{ fontFamily: 'var(--font-barlow-condensed)' }}
              >
                YOUR BUILD
              </h3>
              <div className="bg-[#1a1a1a] rounded-sm border border-[rgba(242,242,238,0.12)] p-6 flex flex-col gap-4">
                {(['chassis', 'motor', 'discos', 'sourcing'] as const).map((cat, i) => {
                  const stepForCat = (['chassis', 'motor', 'discos', 'sourcing'] as const).indexOf(cat) + 1
                  return (
                    <div key={cat}>
                      {i > 0 && <Separator className="mb-4 bg-[rgba(242,242,238,0.12)]" />}
                      <div className="flex justify-between items-center">
                        <span className="text-sm uppercase tracking-widest text-[#888880]">{cat}</span>
                        <div className="flex items-center gap-4">
                          <span
                            className="text-base font-black uppercase text-[#f2f2ee]"
                            style={{ fontFamily: 'var(--font-barlow-condensed)' }}
                          >
                            {selections[cat] ? getOptionLabel(cat, selections[cat]!) : '—'}
                          </span>
                          <button
                            type="button"
                            onClick={() => goToStep(stepForCat)}
                            className="text-sm uppercase tracking-widest text-[#cc2200] hover:text-[#a81c00] transition-colors"
                            style={{ fontFamily: 'var(--font-barlow-condensed)' }}
                          >
                            CHANGE
                          </button>
                        </div>
                      </div>
                    </div>
                  )
                })}
                <Separator className="bg-[rgba(242,242,238,0.12)]" />
                <p className="text-sm text-[#888880]">AU$18,000 – AU$25,000</p>
              </div>
            </div>

            {/* Zone B — Contact form */}
            <div>
              <h3
                className="text-3xl font-black uppercase text-[#f2f2ee] mb-6"
                style={{ fontFamily: 'var(--font-barlow-condensed)' }}
              >
                YOUR DETAILS
              </h3>
              <form action={formAction} className="flex flex-col gap-6" noValidate>
                {/* Name */}
                <div className="flex flex-col gap-2">
                  <Label htmlFor="cfg-name" className="text-xs uppercase tracking-widest text-[#f2f2ee]">Name</Label>
                  <Input
                    id="cfg-name"
                    name="name"
                    required
                    maxLength={100}
                    aria-invalid={!!errors.name}
                    aria-describedby={errors.name ? 'cfg-name-error' : undefined}
                    className="bg-[rgba(242,242,238,0.06)] border border-[rgba(242,242,238,0.12)] text-[#f2f2ee] rounded-sm focus-visible:border-[#cc2200] focus-visible:ring-1 focus-visible:ring-[#cc2200]"
                  />
                  {errors.name && (
                    <p id="cfg-name-error" className="text-sm text-[oklch(0.65_0.22_27)]">{errors.name[0]}</p>
                  )}
                </div>
                {/* Email */}
                <div className="flex flex-col gap-2">
                  <Label htmlFor="cfg-email" className="text-xs uppercase tracking-widest text-[#f2f2ee]">Email</Label>
                  <Input
                    id="cfg-email"
                    name="email"
                    type="email"
                    required
                    maxLength={254}
                    aria-invalid={!!errors.email}
                    aria-describedby={errors.email ? 'cfg-email-error' : undefined}
                    className="bg-[rgba(242,242,238,0.06)] border border-[rgba(242,242,238,0.12)] text-[#f2f2ee] rounded-sm focus-visible:border-[#cc2200] focus-visible:ring-1 focus-visible:ring-[#cc2200]"
                  />
                  {errors.email && (
                    <p id="cfg-email-error" className="text-sm text-[oklch(0.65_0.22_27)]">{errors.email[0]}</p>
                  )}
                </div>
                {/* Message — optional */}
                <div className="flex flex-col gap-2">
                  <Label htmlFor="cfg-message" className="text-xs uppercase tracking-widest text-[#f2f2ee]">
                    Message <span className="text-[#888880] normal-case tracking-normal">(optional)</span>
                  </Label>
                  <Textarea
                    id="cfg-message"
                    name="message"
                    rows={4}
                    maxLength={2000}
                    placeholder="Any additional details or questions"
                    aria-invalid={!!errors.message}
                    aria-describedby={errors.message ? 'cfg-message-error' : undefined}
                    className="bg-[rgba(242,242,238,0.06)] border border-[rgba(242,242,238,0.12)] text-[#f2f2ee] rounded-sm focus-visible:border-[#cc2200] focus-visible:ring-1 focus-visible:ring-[#cc2200] placeholder:text-[#888880]"
                  />
                  {errors.message && (
                    <p id="cfg-message-error" className="text-sm text-[oklch(0.65_0.22_27)]">{errors.message[0]}</p>
                  )}
                </div>

                <SubmitButton />

                <div aria-live="polite" aria-atomic="true" className="min-h-[1.5rem]">
                  {state.status === 'error' && state.message && (
                    <p className="text-sm text-[oklch(0.65_0.22_27)]">{state.message}</p>
                  )}
                </div>
              </form>
            </div>
          </div>
        )}
      </div>

      {/* Navigation buttons */}
      {/* Mobile: fixed bottom, stacked. Desktop: static, side by side. */}
      <div className="mt-8 md:mt-12">
        {/* Desktop nav */}
        <div className="hidden md:flex justify-between items-center">
          {currentStep > 1 ? (
            <button
              type="button"
              onClick={handleBack}
              className="text-sm font-black uppercase tracking-widest text-[#888880] hover:text-[#cc2200] transition-colors min-h-[44px] px-4"
              style={{ fontFamily: 'var(--font-barlow-condensed)' }}
            >
              ← Back
            </button>
          ) : (
            <div />
          )}
          {currentStep < 5 && (() => {
            const category = STEP_CATEGORIES[currentStep]
            const isDisabled = selections[category] === null
            return (
              <button
                type="button"
                onClick={isDisabled ? undefined : handleNext}
                aria-disabled={isDisabled}
                className={cn(
                  'text-sm font-black uppercase tracking-widest text-[#f2f2ee] bg-[#cc2200] hover:bg-[#a81c00] transition-colors min-h-[44px] min-w-[200px] px-6 rounded-sm',
                  isDisabled && 'opacity-40 cursor-not-allowed hover:bg-[#cc2200]'
                )}
                style={{ fontFamily: 'var(--font-barlow-condensed)' }}
              >
                NEXT: {STEP_NAMES[currentStep + 1]} →
              </button>
            )
          })()}
        </div>
        {/* Mobile: fixed bottom sticky nav */}
        <div className="md:hidden fixed bottom-0 left-0 right-0 bg-[#0a0a0a] border-t border-[rgba(242,242,238,0.12)] p-4 flex flex-col gap-2 z-10">
          {currentStep < 5 && (() => {
            const category = STEP_CATEGORIES[currentStep]
            const isDisabled = selections[category] === null
            return (
              <button
                type="button"
                onClick={isDisabled ? undefined : handleNext}
                aria-disabled={isDisabled}
                className={cn(
                  'w-full text-sm font-black uppercase tracking-widest text-[#f2f2ee] bg-[#cc2200] hover:bg-[#a81c00] transition-colors min-h-[44px] px-6 rounded-sm',
                  isDisabled && 'opacity-40 cursor-not-allowed hover:bg-[#cc2200]'
                )}
                style={{ fontFamily: 'var(--font-barlow-condensed)' }}
              >
                NEXT: {STEP_NAMES[currentStep + 1]} →
              </button>
            )
          })()}
          {currentStep > 1 && (
            <button
              type="button"
              onClick={handleBack}
              className="w-full text-sm font-black uppercase tracking-widest text-[#888880] hover:text-[#cc2200] transition-colors min-h-[44px] px-4"
              style={{ fontFamily: 'var(--font-barlow-condensed)' }}
            >
              ← Back
            </button>
          )}
        </div>
        {/* Bottom padding so content is not hidden behind mobile sticky nav */}
        <div className="h-32 md:hidden" aria-hidden="true" />
      </div>

      {/* No-selection guard — shown below nav on steps 1-4 when nothing selected */}
      {currentStep < 5 && selections[STEP_CATEGORIES[currentStep]] === null && (
        <p className="mt-2 text-sm text-[#888880] text-center md:text-right">
          Select an option to continue
        </p>
      )}
    </div>
  )
}
```

CRITICAL PITFALLS to avoid (verified in RESEARCH.md — do not skip reading these):
- Pitfall 1: `.bind()` MUST be inside the component function body, not at module level
- Pitfall 2: Server Action signature is `(selections, _prevState, formData)` — THREE args, selections first
- Pitfall 3: Use `direction` state to pick animation class; do not use same class for both directions
- Pitfall 4: `goToStep(n)` updates ONLY currentStep, never resets selections
- Pitfall 5: Next button uses `aria-disabled` (not HTML `disabled`) and returns early on click when disabled
- Pitfall 6: `SubmitButton` MUST be a separate function component, not inline in the form render
  </action>
  <verify>
    <automated>cd /Users/Mendii/Desktop/proyectos/lambretta-bul && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "'use client'" components/configurator-wizard.tsx returns line 1
    - grep "ConfiguratorWizard" components/configurator-wizard.tsx returns export declaration
    - grep ".bind(null, selections)" components/configurator-wizard.tsx returns 1 line inside component body
    - grep "useFormStatus" components/configurator-wizard.tsx returns a line inside a separate SubmitButton function
    - grep "role=\"radiogroup\"" components/configurator-wizard.tsx returns a line
    - grep "role=\"radio\"" components/configurator-wizard.tsx returns a line
    - grep "slide-in-from-right-4" components/configurator-wizard.tsx returns a line
    - grep "slide-in-from-left-4" components/configurator-wizard.tsx returns a line
    - grep "INQUIRY SENT" components/configurator-wizard.tsx returns a line (success heading)
    - grep "CHANGE" components/configurator-wizard.tsx returns at least 1 line (summary change button)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>components/configurator-wizard.tsx exports ConfiguratorWizard, implements all 5 steps with correct ARIA, direction-aware animations, .bind() pattern, and success state. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create app/configure/page.tsx and run full build</name>
  <files>app/configure/page.tsx</files>
  <read_first>
    - app/contact/page.tsx — exact page.tsx analog to mirror; note that configure/page.tsx does NOT need connection() or searchParams
    - .planning/phases/02-configurator-and-inquiry-integration/02-UI-SPEC.md — Component Contract 1 (Page Shell): heading text, price reference copy, max-w-2xl container
    - .planning/phases/02-configurator-and-inquiry-integration/02-PATTERNS.md — "app/configure/page.tsx" section
    - AGENTS.md — read before writing any Next.js Server Component pattern
  </read_first>
  <action>
Create app/configure/page.tsx. This is a Server Component (no 'use client') that imports configuratorOptions from lib/configurator.ts and passes them as a prop to ConfiguratorWizard.

No searchParams, no connection() call — configure page has no dynamic query input.

```typescript
import { Suspense } from 'react'
import { ConfiguratorWizard } from '@/components/configurator-wizard'
import { configuratorOptions } from '@/lib/configurator'

function ConfigureContent() {
  return (
    <>
      <header className="mb-10">
        <h1 className="uppercase">BUILD YOUR LAMBRE-BULL</h1>
        <p className="mt-2 text-sm text-[#888880]">
          Specify your chassis, engine, and components. We build it by hand in Malaga.
        </p>
        <p className="mt-1 text-sm text-[#888880]">from AU$18,000 to AU$25,000</p>
      </header>
      <ConfiguratorWizard options={configuratorOptions} />
    </>
  )
}

export default function ConfigurePage() {
  return (
    <main className="max-w-2xl mx-auto px-4 md:px-8 py-16">
      <Suspense fallback={null}>
        <ConfigureContent />
      </Suspense>
    </main>
  )
}
```

The h1 will pick up the global Barlow Condensed style from globals.css (h1 { font-family: var(--font-barlow-condensed); }) automatically — do NOT add inline fontFamily on the h1.

The max-w-2xl (per UI-SPEC) is wider than the contact page max-w-lg to accommodate the 2-column card grid.

After creating this file, run the full build to confirm the entire wave compiles:
```
npm run build
```
  </action>
  <verify>
    <automated>cd /Users/Mendii/Desktop/proyectos/lambretta-bul && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "ConfiguratorWizard" app/configure/page.tsx returns the import line and the JSX usage
    - grep "configuratorOptions" app/configure/page.tsx returns the import line and the prop
    - grep "use client" app/configure/page.tsx returns no output (this is a Server Component)
    - grep "BUILD YOUR LAMBRE-BULL" app/configure/page.tsx returns 1 line
    - grep "max-w-2xl" app/configure/page.tsx returns 1 line
    - npm run build exits with code 0 (no compilation or rendering errors across all files)
  </acceptance_criteria>
  <done>app/configure/page.tsx created as Server Component, imports and passes configuratorOptions to ConfiguratorWizard, heading and price reference present, npm run build passes green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>5-step configurator wizard at /configure: option card selection, free navigation between steps, summary review with CHANGE links, contact form, submission to Server Action, success state. Nav and mobile menu include "Custom build" link.</what-built>
  <how-to-verify>
    Run: npm run dev

    Then test these behaviors in the browser:

    1. Visit http://localhost:3000 — confirm "Custom build" link appears in desktop nav between Bikes and About
    2. Open mobile menu (resize to mobile) — confirm "Custom build" appears in overlay menu
    3. Navigate to http://localhost:3000/configure — confirm: "BUILD YOUR LAMBRE-BULL" heading, price reference "from AU$18,000 to AU$25,000", "STEP 1 OF 5 — CHASSIS" progress indicator
    4. Step 1: confirm 2 chassis option cards appear. Confirm Next button is disabled (opacity-40) before selection. Select one card — confirm red ring appears and Next button activates.
    5. Click Next — confirm slide-in animation from right, step advances to MOTOR. Back button appears.
    6. Click Back — confirm slide-in animation from LEFT (reverse direction), returns to CHASSIS with selection preserved.
    7. Complete steps 1-4 with any selections. Reach Step 5.
    8. Step 5: confirm Zone A "YOUR BUILD" summary shows all 4 selections. Click any CHANGE link — confirm navigates back to that step WITHOUT clearing other selections. Return to Step 5.
    9. Step 5 Zone B: confirm Name (required), Email (required), Message (optional) fields with correct labels.
    10. Submit with empty fields — confirm validation errors appear for Name and Email.
    11. Fill in Name and Email, click SUBMIT INQUIRY. If RESEND_API_KEY is configured in .env.local: confirm success state appears with "INQUIRY SENT" heading and configuration summary. If not configured: confirm error state appears (not a crash, not a blank screen).
    12. In success state: confirm "← Back to Bikes" link returns to /.
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors are correct, or describe which step failed and what happened instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Client Component | User interaction with option cards and nav buttons — all state is ephemeral, no persistence |
| Client Component→Server Action | formData (name, email, message) and .bind() selections cross the client-server boundary — both untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | configurator-wizard.tsx — option card click handler | accept | onClick sets a string ID from the static options array. No user-controlled string is written to selections state — only option.id values from the imported configuratorOptions const. |
| T-02-09 | Tampering | configurator-wizard.tsx — Next button with aria-disabled | mitigate | Next button click handler checks `selections[category] === null` and returns early. aria-disabled="true" signals to assistive tech. Server Action validates all selections server-side via configSchema as the authoritative gate. |
| T-02-10 | Information Disclosure | app/configure/page.tsx — configuratorOptions in Server Component | accept | configuratorOptions is static placeholder data, not sensitive. Passing it as a prop to a Client Component is standard Next.js pattern; only serializable values are passed. |
| T-02-11 | Elevation of Privilege | .bind() selections crossing client-server boundary | mitigate | Selections are validated by configSchema in the Server Action. Even if a client crafts arbitrary values, Zod validates them as strings and getOptionLabel falls back to the raw ID if not found. Email uses text: only — no HTML rendering risk. |
</threat_model>

<verification>
After all tasks:
- npm run build passes with zero errors
- /configure page renders with correct heading and price reference
- All 5 steps navigable in browser
- Option cards have correct ARIA roles (role=radiogroup, role=radio, aria-checked)
- Success state appears after submission (manual verification with configured env vars)
- No green used anywhere in success state (brand constraint)
</verification>

<success_criteria>
- Buyer can complete the full flow: select chassis, motor, discos, sourcing, review summary, submit contact details, see success state
- Free navigation between steps preserves all selections (CONF-03)
- Email received by seller includes all 4 configuration categories and contact details (CONF-04)
- npm run build passes
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-configurator-and-inquiry-integration/02-03-SUMMARY.md`
</output>
